apiVersion: v1
kind: Service
metadata:
  name: {{ include "recurring.fullname" . }}
  labels:
    {{- include "recurring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "recurring.selectorLabels" . | nindent 4 }}
